<div class="jumbotron">
  <h1>Hello, world!</h1>
  <p>...</p>
  <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a></p>
</div>
<div class="row">
	<% @categories.each do |category| %>
		<div class="col-sm-4">
			<div class="thumbmail">
				<%= link_to image_tag(category.image, class: "img-responsive image"), items_url(:category_id => category.id) %><div class="caption">
					<h1><%= link_to category.name, items_url(:category_id => category.id) %></h1>
				</div>
			</div>
		</div>
	<% end %>
</div>

